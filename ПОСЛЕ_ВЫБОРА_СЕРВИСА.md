# ✅ После выбора сервиса `eventpublic`

## 📋 Что нужно сделать:

### Шаг 1: Выберите `eventpublic`

Используйте стрелки ↑↓ чтобы выбрать `eventpublic`, затем нажмите Enter.

---

### Шаг 2: Проверка связи

После выбора выполните:

```bash
npx -y @railway/cli status
```

Должно показать:
```
Project: joyful-miracle
Environment: production
Service: eventpublic
```

---

### Шаг 3: Проверка переменных

```bash
npx -y @railway/cli variables
```

Должны быть:
- ✅ `DATABASE_URL` (из Postgres-4hJW)
- ✅ `REDIS_URL` (из Redis)
- ❓ Остальные переменные (нужно добавить)

---

### Шаг 4: Установка недостающих переменных

Выполните:

```bash
cd /Users/varya.malinina.2003mail.ru/event_app_new
./railway-auto-setup.sh
```

Это автоматически установит все необходимые переменные.

---

### Шаг 5: Проверка в веб-интерфейсе

1. Откройте https://railway.app
2. Проект `joyful-miracle`
3. Сервис `eventpublic`
4. Settings → Root Directory должен быть `server`
   - Если нет - установите: `server`
5. Variables → проверьте переменные

---

### Шаг 6: Деплой

```bash
git add .
git commit -m "Railway setup"
git push
```

Railway автоматически задеплоит после push!

---

**Выберите `eventpublic` и нажмите Enter!** 🚀

