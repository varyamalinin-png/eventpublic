# Инструкция: Сборка iOS приложения через Xcode с подключением к Metro (как в Expo)

## Вариант 1: Локальный Metro bundler (рекомендуется)

Это работает так же, как `expo start` - приложение подключается к Metro и вы можете видеть изменения в реальном времени.

### Шаги:

1. **Запустите Metro bundler:**
   ```bash
   cd client
   npm start
   ```
   Или:
   ```bash
   npx expo start
   ```
   
   Metro запустится на `http://localhost:8081`

2. **Откройте проект в Xcode:**
   ```bash
   open ios/iwent.xcworkspace
   ```
   
   ⚠️ **Важно:** Используйте `.xcworkspace`, а не `.xcodeproj`!

3. **Выберите схему сборки:**
   - В верхней панели Xcode выберите схему `iwent`
   - Убедитесь, что выбран **Debug** режим (не Release)
   - Выберите устройство или симулятор

4. **Настройте Scheme (опционально):**
   - Product → Scheme → Edit Scheme
   - Убедитесь, что Build Configuration = **Debug**

5. **Соберите и запустите:**
   - Нажмите **Cmd+R** (Run) или кнопку ▶️
   - Приложение соберется и установится на устройство
   - Оно подключится к Metro bundler, и вы увидите изменения в реальном времени

### Проверка подключения:

После запуска приложения в консоли Xcode вы должны увидеть:
```
🔗 [AppDelegate] DEBUG: Подключаемся к локальному Metro bundler
```

Если видите:
```
⚠️ [AppDelegate] DEBUG: Metro недоступен, используем встроенный bundle
```
То Metro не запущен или недоступен. Убедитесь, что Metro работает на порту 8081.

---

## Вариант 2: Подключение к Metro на виртуальной машине

Если Metro запущен на удаленной машине:

1. **Убедитесь, что Metro доступен с вашего Mac:**
   ```bash
   curl http://158.160.67.4:8081/status
   ```

2. **Откройте проект в Xcode:**
   ```bash
   open ios/iwent.xcworkspace
   ```

3. **Соберите и запустите в Debug режиме:**
   - Выберите Debug конфигурацию
   - Нажмите Cmd+R

4. **Приложение подключится к Metro на VM:**
   В консоли вы увидите:
   ```
   🔗 [AppDelegate] DEBUG: Подключаемся к Metro на VM: http://158.160.67.4:8081/...
   ```

---

## Вариант 3: Встроенный bundle (standalone)

Если вы хотите собрать приложение с встроенным JavaScript bundle (без Metro):

1. **Экспортируйте bundle:**
   ```bash
   cd client
   npx expo export --platform ios --output-dir ../ios-build
   ```

2. **Скопируйте bundle в проект:**
   ```bash
   # Bundle будет автоматически найден в ios/iwent/main.jsbundle
   # или создайте его вручную:
   cp ios-build/_expo/static/js/web/main.js ios/iwent/main.jsbundle
   ```

3. **Соберите в Release режиме:**
   - В Xcode выберите **Release** конфигурацию
   - Product → Scheme → Edit Scheme → Build Configuration = **Release**
   - Нажмите Cmd+B (Build)

4. **Установите на устройство:**
   - Product → Archive (для App Store)
   - Или выберите устройство и нажмите Cmd+R

---

## Полезные команды:

### Проверка порта Metro:
```bash
lsof -i :8081
```

### Остановка процессов на порту 8081:
```bash
kill -9 $(lsof -t -i:8081)
```

### Быстрая сборка через CLI:
```bash
# С Metro bundler (разработка)
npx expo run:ios

# С встроенным bundle (production)
npx expo run:ios --configuration Release
```

---

## Устранение проблем:

### Приложение не подключается к Metro:
1. Проверьте, что Metro запущен: `lsof -i :8081`
2. Убедитесь, что используете **Debug** конфигурацию
3. Проверьте, что порт 8081 не заблокирован файрволом
4. Для симулятора: Metro должен быть доступен на `localhost:8081`
5. Для физического устройства: используйте IP-адрес вашего Mac или туннель

### Ошибки сборки:
1. Очистите кэш: Product → Clean Build Folder (Shift+Cmd+K)
2. Переустановите зависимости: `cd ios && pod install`
3. Удалите DerivedData: `rm -rf ~/Library/Developer/Xcode/DerivedData`

### Использование туннеля для физического устройства:
```bash
npx expo start --tunnel
```
Это создаст публичный URL для Metro, доступный с любого устройства.

---

## Различия между режимами:

| Режим | Metro | Hot Reload | Bundle | Использование |
|-------|-------|------------|--------|---------------|
| **Debug** | ✅ Да | ✅ Да | ❌ Нет | Разработка |
| **Release** | ❌ Нет | ❌ Нет | ✅ Да | Production |


