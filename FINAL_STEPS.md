# ✅ Финальные шаги

## 🎉 Что уже сделано:

1. ✅ `client/app.json` → `apiUrl` = `https://eventpublic-production.up.railway.app`
2. ✅ `client/app.json` → `NSExceptionDomains` = добавлен Railway домен

---

## 📝 Что нужно сделать в Railway:

### Шаг 1: Обновите APP_BACKEND_BASE_URL

1. В Railway откройте ваш backend сервис
2. Перейдите в **"Variables"**
3. Добавьте или обновите:
   ```bash
   APP_BACKEND_BASE_URL=https://eventpublic-production.up.railway.app
   ```
4. Railway автоматически перезапустит сервис

---

## 📱 Обновление приложения:

### Шаг 1: Закоммитьте изменения

```bash
cd /Users/varya.malinina.2003mail.ru/event_app_new

git add client/app.json
git commit -m "Update API URL to Railway"
git push
```

### Шаг 2: Пересоберите приложение в Xcode

1. Откройте Xcode
2. `Product` → `Clean Build Folder` (`Shift + Cmd + K`)
3. Выберите **Release** конфигурацию
4. Выберите **iPhone 14 Pro**
5. Нажмите **Play (▶️)**

---

## 🔍 Проверка:

### 1. Проверьте Railway сервер:

Откройте в браузере:
```
https://eventpublic-production.up.railway.app/health
```

Должен вернуться: `{"status":"ok"}`

### 2. Проверьте PostgreSQL:

В Railway → PostgreSQL → должна быть кнопка **"Query"** - там должны быть таблицы.

---

## ✅ Готово!

После выполнения всех шагов:
- ✅ Сервер работает на Railway 24/7
- ✅ Не нужно держать MacBook включенным
- ✅ Постоянный URL
- ✅ Приложение подключено к удаленному серверу

---

**После обновления APP_BACKEND_BASE_URL в Railway и пересборки приложения - всё готово!** 🚀

