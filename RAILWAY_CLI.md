# 🚂 Настройка Railway через CLI

## Шаг 1: Установка Railway CLI

```bash
curl -fsSL https://railway.app/install.sh | sh
```

Перезапустите терминал после установки.

---

## Шаг 2: Авторизация

```bash
railway login
```

Откроется браузер для авторизации через GitHub.

---

## Шаг 3: Инициализация проекта

```bash
cd /Users/varya.malinina.2003mail.ru/event_app_new/server
railway init
```

Выберите:
- **"Create new project"** или **"Link to existing project"**
- Если создаете новый: название `event-app-backend`
- Если линкуете: выберите существующий проект

---

## Шаг 4: Связывание с PostgreSQL и Redis

Railway автоматически должен связать базы данных, если они в одном проекте.

Если не связаны, посмотрите ID сервисов:

```bash
# Посмотреть все сервисы в проекте
railway service

# Или в Railway веб-интерфейсе скопируйте ID сервисов
```

---

## Шаг 5: Установка переменных окружения

```bash
cd server

# Обязательные переменные
railway variables set NODE_ENV=production
railway variables set PORT=4000
railway variables set CORS_ORIGIN=*

# JWT секреты (используйте сгенерированные ранее)
railway variables set JWT_ACCESS_SECRET=EIGUZBTMbqW2OD2my1Gk9qUdVs3XFo5MgI1YY1aXYTE=
railway variables set JWT_REFRESH_SECRET=oBo5isGfN6UoUEG+cXl1GJDHBpU6RuGoOvyiAWhX2E8=
railway variables set JWT_ACCESS_TTL=15m
railway variables set JWT_REFRESH_TTL=7d

# URL (после получения)
railway variables set APP_BACKEND_BASE_URL=https://eventpublic-production.up.railway.app
```

---

## Шаг 6: Связывание DATABASE_URL и REDIS_URL

Railway автоматически создает эти переменные при создании баз данных.

Чтобы проверить:

```bash
# Посмотреть все переменные
railway variables
```

Если `DATABASE_URL` и `REDIS_URL` не видны, возможно базы данных в другом проекте.

---

## Шаг 7: Настройка Root Directory

```bash
# Установить root directory через CLI
railway run --directory server

# Или в веб-интерфейсе Railway:
# Settings → Root Directory → server
```

---

## Шаг 8: Деплой

```bash
# Загрузить код и задеплоить
railway up
```

Или просто запушьте в GitHub - Railway автоматически задеплоит.

---

## Альтернатива: Через веб-интерфейс

Если CLI не работает, настройте через веб:

1. Откройте https://railway.app
2. Ваш проект → **"+ New"**
3. Выберите **"GitHub Repo"**
4. Выберите репозиторий `eventpublic`
5. В настройках сервиса:
   - **Root Directory:** `server`
   - **Variables:** добавьте все переменные

---

**Попробуйте сначала через веб-интерфейс - это проще!** 🚀

