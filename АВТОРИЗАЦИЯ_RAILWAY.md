# 🔐 Авторизация в Railway CLI

## Что такое `--browserless`?

`--browserless` — это режим авторизации **без открытия браузера**. 

Вместо автоматического открытия браузера:
- Railway CLI покажет вам **ссылоку** и **токен**
- Вы скопируете ссылку, откроете её в браузере вручную
- Вставите токен на странице
- Авторизация завершится

---

## ✅ Способ 1: С браузером (если работает)

```bash
npx -y @railway/cli login
```

Автоматически откроет браузер для авторизации.

---

## ✅ Способ 2: Без браузера (`--browserless`)

```bash
npx -y @railway/cli login --browserless
```

### Что произойдет:

1. CLI покажет вам:
   ```
   Visit this URL to authenticate:
   https://railway.app/cli/login?token=XXXXXXXX
   
   Enter the token:
   ```
   
2. Скопируйте **ссылку** (URL) из терминала

3. Откройте её в браузере

4. Скопируйте **токен** из терминала

5. Вставьте токен на странице Railway

6. Готово! ✅

---

## 🚀 Быстрый способ:

```bash
cd /Users/varya.malinina.2003mail.ru/event_app_new/server
npx -y @railway/cli login --browserless
```

Затем следуйте инструкциям на экране!

