# 🚂 Пошаговая настройка Railway

## ✅ Шаг 1: PostgreSQL - ГОТОВО!

**"You have no tables" - это нормально!** 

Таблицы будут созданы автоматически при первом деплое сервера. Railway запустит команду:
```bash
npm run prisma:deploy
```
Эта команда выполнит все миграции Prisma и создаст все таблицы.

---

## 📋 Что делать дальше:

### Шаг 2: Добавьте Redis

1. В Railway проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add Redis"**
3. Railway создаст Redis автоматически
4. **Скопируйте `REDIS_URL`** (он будет в переменных окружения)

---

### Шаг 3: Настройте сервис (ваш backend)

1. В Railway проекте найдите ваш сервис (backend)
2. Нажмите на него
3. Перейдите в **"Settings"**
4. Установите **"Root Directory"** = `server`

---

### Шаг 4: Добавьте переменные окружения

В настройках вашего сервиса найдите **"Variables"** и добавьте:

#### Обязательные:

```bash
NODE_ENV=production
PORT=4000
CORS_ORIGIN=*

DATABASE_URL=<скопируйте из PostgreSQL - уже есть>
REDIS_URL=<скопируйте из Redis>

JWT_ACCESS_SECRET=EIGUZBTMbqW2OD2my1Gk9qUdVs3XFo5MgI1YY1aXYTE=
JWT_REFRESH_SECRET=oBo5isGfN6UoUEG+cXl1GJDHBpU6RuGoOvyiAWhX2E8=
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d
```

#### Опциональные (для email):

```bash
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_SECURE=false
```

#### Опциональные (для хранения файлов):

```bash
STORAGE_DRIVER=s3
STORAGE_BUCKET=event-app-media
STORAGE_REGION=us-east-1
STORAGE_ACCESS_KEY=<ваш ключ>
STORAGE_SECRET_KEY=<ваш секретный ключ>
STORAGE_PUBLIC_BASE_URL=https://<bucket>.s3.amazonaws.com
STORAGE_FORCE_PATH_STYLE=false
```

**Или оставьте пустым** - приложение будет работать, но загрузка файлов может не работать.

---

### Шаг 5: Дождитесь деплоя

1. Railway автоматически начнет деплой после настройки переменных
2. Дождитесь завершения деплоя (обычно 2-5 минут)
3. После деплоя таблицы будут созданы автоматически

---

### Шаг 6: Получите URL API

1. После успешного деплоя Railway создаст публичный URL
2. Он будет в настройках сервиса → **"Settings"** → **"Domains"**
3. Скопируйте URL (например: `https://xxx.up.railway.app`)

---

### Шаг 7: Обновите APP_BACKEND_BASE_URL

1. В Railway → ваш сервис → **"Variables"**
2. Добавьте/обновите:
   ```bash
   APP_BACKEND_BASE_URL=https://xxx.up.railway.app
   ```
3. Railway автоматически перезапустит сервис

---

### Шаг 8: Обновите приложение

1. Откройте `client/app.json`
2. Обновите:
   ```json
   "extra": {
     "apiUrl": "https://xxx.up.railway.app"
   }
   ```
3. Добавьте домен в `NSExceptionDomains`:
   ```json
   "xxx.up.railway.app": {
     "NSExceptionAllowsInsecureHTTPLoads": false,
     "NSIncludesSubdomains": true
   }
   ```
4. Пересоберите приложение в Xcode

---

## 🔍 Проверка после деплоя

1. Откройте URL вашего Railway проекта + `/health`
   Например: `https://xxx.up.railway.app/health`
2. Должен вернуться JSON: `{"status":"ok"}`
3. Проверьте PostgreSQL - теперь должны быть таблицы!

---

## ⚠️ Важно

- **Таблицы появятся после первого успешного деплоя**
- **Миграции запустятся автоматически** через команду `npm run prisma:deploy`
- Если деплой упал с ошибкой - проверьте логи в Railway

---

**Продолжайте настройку - всё идет правильно!** ✅

