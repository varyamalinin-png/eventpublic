# 🚀 Полная настройка с нуля: GitHub → Railway

## 📋 Ваш GitHub: `varyamalinin-png`

---

## ШАГ 1: Загрузка проекта на GitHub

### 1.1. Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. **Repository name:** `event-app-new` (или любое другое название)
3. Выберите **Private** (рекомендуется)
4. **НЕ** добавляйте README, .gitignore, license (у нас уже всё есть)
5. Нажмите **"Create repository"**

### 1.2. Загрузите код на GitHub

Откройте терминал и выполните:

```bash
cd /Users/varya.malinina.2003mail.ru/event_app_new

# Проверяем статус git
git status

# Добавляем все файлы (если нужно)
git add .

# Делаем коммит (если есть изменения)
git commit -m "Prepare for Railway deployment"

# Подключаем ваш GitHub репозиторий
git remote set-url origin https://github.com/varyamalinin-png/event-app-new.git

# Или если это первый раз:
# git remote add origin https://github.com/varyamalinin-png/event-app-new.git

# Загружаем код на GitHub
git push -u origin main
```

**Если попросит логин/пароль:**
- Используйте GitHub Personal Access Token вместо пароля
- Создайте токен: https://github.com/settings/tokens → "Generate new token (classic)"
- Дайте права: `repo`

---

## ШАГ 2: Настройка Railway

### 2.1. Регистрация на Railway

1. Откройте https://railway.app
2. Нажмите **"Start a New Project"** или **"Login"**
3. Войдите через **GitHub** (нажмите "Login with GitHub")
4. Авторизуйте Railway для доступа к репозиториям

### 2.2. Создание проекта

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите ваш репозиторий `event-app-new`
4. Нажмите на него

### 2.3. Настройка сервиса

Railway попытается автоматически определить проект. Нужно указать:

1. В настройках сервиса найдите **"Root Directory"**
2. Установите: `server`
3. **Build Command** (если есть поле):
   ```bash
   npm install && npm run prisma:generate && npm run build
   ```
4. **Start Command** (если есть поле):
   ```bash
   npm run prisma:deploy && npm run start:prod
   ```

### 2.4. Добавление PostgreSQL

1. В проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст базу данных
4. Скопируйте **`DATABASE_URL`** (он будет в переменных окружения)

### 2.5. Добавление Redis

1. В проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add Redis"**
3. Railway автоматически создаст Redis
4. Скопируйте **`REDIS_URL`** (он будет в переменных окружения)

### 2.6. Настройка переменных окружения

В настройках вашего сервиса (сервера) найдите **"Variables"** и добавьте:

#### Обязательные переменные:

```bash
NODE_ENV=production
PORT=4000
CORS_ORIGIN=*

DATABASE_URL=<скопируйте из PostgreSQL>
REDIS_URL=<скопируйте из Redis>

JWT_ACCESS_SECRET=<сгенерируйте случайную строку минимум 16 символов>
JWT_REFRESH_SECRET=<сгенерируйте другую случайную строку минимум 16 символов>
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d
```

**Как сгенерировать секреты:**
```bash
# В терминале Mac:
openssl rand -base64 32
# Выполните дважды для JWT_ACCESS_SECRET и JWT_REFRESH_SECRET
```

#### Опциональные переменные (для email):

```bash
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_SECURE=false
```

#### Опциональные переменные (для хранения файлов):

Если хотите использовать AWS S3 (бесплатный tier) или другой S3-совместимый сервис:

```bash
STORAGE_DRIVER=s3
STORAGE_BUCKET=event-app-media
STORAGE_REGION=us-east-1
STORAGE_ACCESS_KEY=<ваш ключ>
STORAGE_SECRET_KEY=<ваш секретный ключ>
STORAGE_PUBLIC_BASE_URL=https://<bucket>.s3.amazonaws.com
STORAGE_FORCE_PATH_STYLE=false
```

**Или оставьте пустым** - приложение будет работать, но загрузка файлов может не работать.

### 2.7. Получение URL вашего API

1. После деплоя Railway создаст публичный URL
2. Он будет выглядеть как: `https://your-project-name.up.railway.app`
3. Скопируйте этот URL
4. Обновите переменную окружения:
   ```bash
   APP_BACKEND_BASE_URL=https://your-project-name.up.railway.app
   ```

---

## ШАГ 3: Обновление приложения

### 3.1. Обновить `client/app.json`

1. Откройте `client/app.json`
2. Найдите секцию `"extra"`:
   ```json
   "extra": {
     "apiUrl": "https://your-project-name.up.railway.app"
   }
   ```
3. Замените URL на ваш Railway URL

### 3.2. Обновить `NSExceptionDomains`

В том же файле `client/app.json` найдите `"NSExceptionDomains"` и добавьте:

```json
"your-project-name.up.railway.app": {
  "NSExceptionAllowsInsecureHTTPLoads": false,
  "NSIncludesSubdomains": true
}
```

### 3.3. Закоммитить изменения

```bash
cd /Users/varya.malinina.2003mail.ru/event_app_new

git add client/app.json
git commit -m "Update API URL for Railway"
git push
```

Railway автоматически пересоберет сервер после push.

---

## ШАГ 4: Пересборка приложения в Xcode

1. Откройте Xcode
2. `Product` → `Clean Build Folder` (`Shift + Cmd + K`)
3. Выберите **Release** конфигурацию
4. Выберите **iPhone 14 Pro**
5. Нажмите **Play (▶️)**

---

## ✅ Готово!

После всех шагов:
- ✅ Сервер работает 24/7 на Railway
- ✅ Не нужно держать MacBook включенным
- ✅ Постоянный URL (не меняется)
- ✅ Автоматический деплой при каждом push в GitHub

---

## 🔍 Проверка

1. Откройте URL вашего Railway проекта в браузере
2. Добавьте `/health` в конец URL (например: `https://your-project.up.railway.app/health`)
3. Должен вернуться JSON с `"status": "ok"`

---

## 📝 Полезные команды

### Посмотреть логи на Railway:
- Откройте проект на Railway → выберите сервис → вкладка "Deployments" → "View Logs"

### Перезапустить сервис:
- На Railway → сервис → "Settings" → "Restart"

### Обновить переменные окружения:
- На Railway → сервис → "Variables" → добавьте/измените переменные → автоматически перезапустится

---

**Удачи! После настройки ваш сервер будет работать независимо от MacBook.** 🚀

