// Заглушка для expo-image-manipulator
export const manipulateAsync = async (uri, actions) => ({ uri });

export const SaveFormat = {};
export const FlipType = {};

export default {
  manipulateAsync,
  SaveFormat,
  FlipType,
};

