# 🔍 Диагностика белого экрана на вебе

## Что было исправлено:

### 1. Обработка ошибок в explore.tsx
- ✅ Добавлена проверка `Platform.OS !== 'web'` для Haptics (не работает на вебе)
- ✅ Добавлены try-catch блоки для всех вычислений с массивами
- ✅ Добавлены проверки `Array.isArray()` перед использованием `.map()/.filter()`
- ✅ Добавлена валидация событий и организаторов перед рендерингом
- ✅ Добавлена обработка ошибок в `getOrganizersForEvents`
- ✅ Добавлена обработка ошибок в `folders` calculation
- ✅ Добавлена обработка ошибок в `globEvents`, `friendsEvents`, `folderEvents`
- ✅ Добавлен `.filter(Boolean)` для удаления null элементов
- ✅ Добавлена обработка ошибок в фильтрах (время, цена, возраст)
- ✅ Добавлена обработка ошибок в загрузке профилей событий
- ✅ Отключен Alert на вебе (может вызывать проблемы)

### 2. Исправлены ошибки TypeScript
- ✅ Удален дублирующийся экспорт типов в `getSwipeButtons.ts`
- ✅ Исправлены логические ошибки в `event-profile/[id].tsx`
- ✅ Удален дублирующийся импорт в `RequestMiniCard.tsx`
- ✅ Исправлен тип `isDisabled` в `OutgoingRequestMiniCard.tsx`

## Как проверить, что исправления работают:

### 1. Откройте консоль браузера (F12)
Проверьте наличие ошибок:
- Если есть ошибки - скопируйте их и отправьте мне
- Если нет ошибок, но экран белый - см. пункт 2

### 2. Проверьте Network tab
- Убедитесь, что все запросы к API проходят успешно
- Проверьте, что WebSocket подключается (может быть ошибка, но это нормально)

### 3. Проверьте React DevTools
- Установите React DevTools extension
- Проверьте, что компоненты рендерятся
- Проверьте состояние контекстов (EventsContext, AuthContext)

### 4. Проверьте localStorage
В консоли браузера выполните:
```javascript
// Проверьте токены
localStorage.getItem('auth.accessToken')
localStorage.getItem('auth.refreshToken')

// Проверьте активный аккаунт
localStorage.getItem('auth.activeAccountId.v1')
```

## Возможные причины белого экрана:

1. **Ошибка инициализации контекста**
   - Проверьте консоль на ошибки типа "Cannot access before initialization"
   - Решение: уже добавлена обработка в `_layout.web.tsx`

2. **Ошибка загрузки данных**
   - Проверьте, что `events` загружаются
   - Решение: добавлены проверки и fallback на пустой массив

3. **Ошибка рендеринга компонента**
   - Проверьте консоль на ошибки рендеринга
   - Решение: добавлены try-catch и валидация данных

4. **Проблемы с WebSocket**
   - WebSocket может не работать на вебе
   - Решение: ошибки WebSocket подавляются в `_layout.web.tsx`

5. **Проблемы с SecureStore на вебе**
   - `expo-secure-store` может не работать на вебе
   - Решение: нужно проверить, используется ли он правильно

## Следующие шаги для диагностики:

Если экран все еще белый:

1. **Откройте консоль браузера** и проверьте ошибки
2. **Проверьте Network tab** - все ли запросы проходят
3. **Проверьте React DevTools** - рендерятся ли компоненты
4. **Отправьте мне скриншот консоли** с ошибками

## Быстрая проверка:

Откройте в браузере: `https://iventapp.ru/explore`
- Откройте DevTools (F12)
- Перейдите на вкладку Console
- Посмотрите, есть ли ошибки
- Отправьте мне скриншот или текст ошибок
