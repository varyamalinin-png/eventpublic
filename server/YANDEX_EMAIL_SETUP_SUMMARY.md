# 📧 Настройка Yandex Cloud Email API - Краткое руководство

## ✅ Текущий статус

### Что уже настроено:
- ✅ Скрипт для получения IAM токена (`scripts/get-yandex-iam-token.js`)
- ✅ Скрипт для проверки конфигурации (`scripts/check-yandex-email-config.js`)
- ✅ MailerService использует только Yandex Cloud Email API
- ✅ Улучшена обработка ошибок и логирование

### Что нужно сделать:

## 🚀 Быстрая настройка (3 шага)

### Шаг 1: Получить IAM токен

```bash
cd server
node scripts/get-yandex-iam-token.js
```

**Скопируйте полученный токен!** Пример:
```
YANDEX_IAM_TOKEN=<YOUR_IAM_TOKEN_HERE>
Получите токен: node scripts/get-yandex-iam-token.js
```

⚠️ **Внимание**: Токен действителен только 12 часов!

### Шаг 2: Установить переменные в Railway

#### Вариант А: Через Railway Dashboard (рекомендуется)

1. Перейдите на https://railway.app
2. Выберите проект → сервис **eventpublic**
3. Перейдите в раздел **Variables**
4. Нажмите **+ New Variable** и добавьте:

   | Переменная | Значение |
   |------------|----------|
   | `YANDEX_IAM_TOKEN` | (токен из шага 1) |
   | `YANDEX_CLOUD_FROM_EMAIL` | `noreply@iventapp.ru` |
   | `YANDEX_CLOUD_API_ENDPOINT` | `https://mail-api.cloud.yandex.net` (опционально) |
   | `EMAIL_VERIFICATION_REDIRECT_URL` | `https://eventpublic-production.up.railway.app/auth/verify-email` (опционально) |

#### Вариант Б: Через Railway CLI

```bash
# Установить Railway CLI (если еще не установлен)
npm install -g @railway/cli

# Войти в Railway
railway login

# Установить переменные
railway variables set YANDEX_IAM_TOKEN="ваш_токен_из_шага_1" --service eventpublic
railway variables set YANDEX_CLOUD_FROM_EMAIL="noreply@iventapp.ru" --service eventpublic
```

### Шаг 3: Проверить настройку

```bash
cd server
node scripts/check-yandex-email-config.js
```

Если все настроено правильно, вы увидите:
```
✅ Все обязательные переменные установлены!
✅ IAM токен валиден
✅ Конфигурация Yandex Cloud Email API полностью настроена и работает!
```

## 📋 Необходимые переменные окружения

### Обязательные:

1. **YANDEX_IAM_TOKEN**
   - Получается через: `node scripts/get-yandex-iam-token.js`
   - Действителен 12 часов
   - Нужно обновлять регулярно

2. **YANDEX_CLOUD_FROM_EMAIL**
   - Значение: `noreply@iventapp.ru`
   - Домен должен быть подтвержден в Yandex Cloud

### Опциональные:

3. **YANDEX_CLOUD_API_ENDPOINT**
   - По умолчанию: `https://mail-api.cloud.yandex.net`
   - Менять не нужно, если не используете другой endpoint

4. **EMAIL_VERIFICATION_REDIRECT_URL**
   - URL для редиректа после подтверждения email
   - Пример: `https://eventpublic-production.up.railway.app/auth/verify-email`

5. **PASSWORD_RESET_REDIRECT_URL**
   - URL для редиректа после сброса пароля

## ⚠️ Важные замечания

### IAM токен действителен 12 часов

Для продакшена нужно настроить автоматическое обновление токена. Два варианта:

1. **Cron job на сервере** - запускать `get-yandex-iam-token.js` каждые 11 часов
2. **Автоматическое обновление в приложении** - использовать авторизованный ключ вместо токена

### Подтверждение домена

Убедитесь, что домен `iventapp.ru` подтвержден в Yandex Cloud:

1. Перейдите в [Yandex Cloud Console](https://console.cloud.yandex.ru)
2. Откройте раздел **Email**
3. Добавьте домен `iventapp.ru`
4. Настройте DNS записи согласно инструкциям

## 🔍 Проверка работы

После установки переменных:

1. **Перезапустите сервис в Railway**
2. **Проверьте логи** - должна появиться запись:
   ```
   ✅ Yandex Cloud Email API enabled (from: noreply@iventapp.ru)
   ```
3. **Зарегистрируйте тестового пользователя**
4. **Проверьте, что письмо отправляется**

## 🛠️ Устранение проблем

### "Yandex Cloud Email API is not configured"
- Проверьте, что переменные установлены в Railway
- Перезапустите сервис после установки переменных

### "IAM токен невалиден"
- Получите новый токен: `node scripts/get-yandex-iam-token.js`
- Установите новый токен в Railway
- Убедитесь, что токен скопирован полностью (без переносов строк)

### Письма не доходят
- Проверьте папку "Спам"
- Проверьте, что домен подтвержден в Yandex Cloud
- Проверьте логи сервера на наличие ошибок

## 📚 Дополнительная документация

См. [YANDEX_EMAIL_SETUP.md](./YANDEX_EMAIL_SETUP.md) для детальной документации.

## 🎯 Текущий IAM токен

**Получите новый токен:**
```bash
cd server
node scripts/get-yandex-iam-token.js
```

**Токен действителен 12 часов!**

