# 🔧 Решение проблемы DNS: ENOTFOUND mail-api.cloud.yandex.net

## Проблема

Railway не может разрешить DNS имя `mail-api.cloud.yandex.net`:
```
getaddrinfo ENOTFOUND mail-api.cloud.yandex.net
```

## Возможные причины

1. **Railway блокирует доступ к российским доменам** (регион us-east4)
2. **DNS серверы Railway не могут разрешить .yandex.net домены**
3. **Сетевые ограничения на Railway**

## Решения

### Вариант 1: Использовать IP адрес вместо домена

Нужно найти IP адрес для `mail-api.cloud.yandex.net` и использовать его напрямую.

### Вариант 2: Использовать другой endpoint

Проверить, есть ли альтернативный endpoint для Yandex Cloud Email API.

### Вариант 3: Настроить VPN или прокси (сложно)

Использовать прокси для доступа к Yandex Cloud API.

### Вариант 4: Сменить регион Railway

Переместить сервис в другой регион (например, в Европу), который может иметь доступ.

### Вариант 5: Использовать альтернативный email сервис

Если доступ к Yandex Cloud невозможен, использовать:
- SendGrid
- Mailgun (но у вас Account disabled)
- Resend
- AWS SES

## Рекомендация

**Самый простой вариант:** Использовать Resend или другой email сервис, который работает из региона Railway.

## Временное решение

Пока проблема не решена, можно:
1. Использовать другой email сервис
2. Или настроить VPN/прокси для доступа к Yandex Cloud

## Проверка доступности домена

Попробуйте выполнить на сервере Railway:
```bash
nslookup mail-api.cloud.yandex.net
# или
dig mail-api.cloud.yandex.net
```

Если команда не возвращает IP - значит проблема с DNS на Railway.

