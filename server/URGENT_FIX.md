# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ - Новая версия на сервере

## Проблема

На сервере Railway запущена **СТАРАЯ версия кода**, которая содержит:
- ❌ Mailgun (не работает - Account disabled)
- ❌ SMTP (Connection timeout)
- ❌ Использует `fetch` (падает с "fetch failed")

## Что было исправлено

✅ **Удален весь код Mailgun/SMTP** - теперь используется **ТОЛЬКО Yandex Cloud Email API**
✅ **Заменен `fetch` на встроенный `https` модуль** - для совместимости
✅ **Улучшена обработка ошибок** - детальное логирование

## Что нужно сделать ПРЯМО СЕЙЧАС

### 1. Очистить старую сборку на Railway

В Railway нужно **полностью пересобрать проект**:

```bash
# На Railway в настройках проекта:
# 1. Settings → Build → Clear Build Cache
# 2. Redeploy
```

### 2. Проверить переменные окружения

Убедитесь, что установлены:
- `YANDEX_IAM_TOKEN` - токен IAM (действителен 12 часов!)
- `YANDEX_CLOUD_FROM_EMAIL` - noreply@iventapp.ru
- `YANDEX_CLOUD_API_ENDPOINT` - https://mail-api.cloud.yandex.net (опционально)

### 3. После деплоя проверить логи

В логах должны появиться:
```
✅ Yandex Cloud Email API enabled (from: noreply@iventapp.ru)
[MailerService] ✅ Using Yandex Cloud Email API to send email to ...
```

**НЕ должно быть:**
- ❌ "Mailgun check"
- ❌ "Using Mailgun API"
- ❌ "Using SMTP"
- ❌ "fetch failed"

## Важно

**IAM токен действителен только 12 часов!** Если он истек:
```bash
cd server && node scripts/get-yandex-iam-token.js
```

## Как проверить что все работает

1. Зарегистрировать нового пользователя
2. Проверить логи - должны быть только сообщения о Yandex Cloud
3. Проверить почту - письмо должно прийти

## Если проблемы остались

Проверить версию на сервере:
```bash
# В логах должно быть:
# - "Using Yandex Cloud Email API" ✅
# - НЕТ "Mailgun" или "SMTP" ✅
```

Если все еще видны Mailgun/SMTP - значит старая версия все еще запущена!

