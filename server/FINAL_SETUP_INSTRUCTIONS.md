# 🚀 Финальная настройка приложения

## ✅ Что уже сделано

1. ✅ VM создана и запущена (IP: 51.250.105.190)
2. ✅ PostgreSQL установлен и настроен
3. ✅ Redis установлен
4. ✅ Node.js, npm, PM2 установлены
5. ✅ Репозиторий клонирован
6. ✅ Зависимости установлены
7. ✅ .env файл создан
8. ✅ Приложение собрано
9. ✅ Firewall настроен (порты 4000, 8081 открыты)

## ⚠️ Что нужно доделать

### 1. Подключиться к VM через веб-консоль Yandex Cloud

Если SSH не работает:
1. Откройте [Yandex Cloud Console](https://console.cloud.yandex.ru)
2. Перейдите в Compute Cloud → Виртуальные машины
3. Найдите `event-app-backend`
4. Нажмите "Подключиться" → "SSH"
5. Используйте приватный ключ из `~/.ssh/yandex-cloud`

### 2. Завершить настройку базы данных

В веб-консоли выполните:

```bash
cd ~/event_app_new/server

# Запустить PostgreSQL
sudo systemctl start postgresql
sudo systemctl enable postgresql

# Применить миграции
npx prisma db push --accept-data-loss

# Или через миграции
npx prisma migrate deploy
```

### 3. Запустить приложение

```bash
cd ~/event_app_new/server

# Запустить через PM2
pm2 start dist/main.js --name event-app

# Сохранить конфигурацию
pm2 save

# Настроить автозапуск
pm2 startup systemd -u ubuntu --hp /home/ubuntu
# Скопируйте и выполните команду, которую выдаст PM2
```

### 4. Проверить работу API

```bash
# Проверить статус
pm2 status

# Посмотреть логи
pm2 logs event-app

# Проверить health endpoint
curl http://localhost:4000/health
```

### 5. Настроить Yandex Cloud Email API

Добавьте в `.env` файл:

```bash
YANDEX_IAM_TOKEN=ваш_iam_токен
YANDEX_CLOUD_FROM_EMAIL=noreply@iventapp.ru
```

### 6. Настроить доступ извне

Проверьте, что порты открыты в Yandex Cloud Security Groups:
- Порт 22 (SSH)
- Порт 4000 (API)
- Порт 8081 (Frontend, если будет)

## 📱 Настройка для мобильного приложения

### Backend URL

В мобильном приложении укажите:
```
http://51.250.105.190:4000
```

### CORS

Убедитесь, что в `.env`:
```
CORS_ORIGIN=*
```

## 🔧 Полезные команды

```bash
# Проверить статус приложения
pm2 status

# Посмотреть логи
pm2 logs event-app

# Перезапустить приложение
pm2 restart event-app

# Остановить приложение
pm2 stop event-app

# Проверить базу данных
sudo -u postgres psql -d event_app -c "\dt"

# Проверить Redis
redis-cli ping
```

## 🌐 Доступ к API

После запуска приложение будет доступно по адресу:
- **API**: http://51.250.105.190:4000
- **Health check**: http://51.250.105.190:4000/health

## 📝 Следующие шаги

1. Настроить домен (опционально)
2. Настроить SSL/HTTPS (опционально)
3. Настроить Object Storage для файлов (опционально)
4. Настроить мониторинг и логирование

