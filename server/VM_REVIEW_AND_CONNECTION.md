# 📊 Обзор виртуальной машины и подключение

## ✅ Анализ вашей VM

### Что хорошо:
- ✅ **Статус**: Running (работает)
- ✅ **Публичный IP**: 51.250.105.190 (есть)
- ✅ **Зона**: ru-central1-b (правильная)
- ✅ **Ресурсы**: 2 vCPU, 4GB RAM, 20GB диск (достаточно)
- ✅ **ОС**: Ubuntu 22.04 LTS (подходит)
- ✅ **Тарификация**: Free (пробный период)

### Что нужно проверить:
- ⚠️ **OS Login**: Выключен (это нормально, мы используем SSH ключ)
- ⚠️ **Security Groups**: Нужно проверить, открыты ли порты 4000 и 8081

## 🔌 Как подключиться

### ❌ НЕ нажимайте "Cloud Shell"

**Cloud Shell** - это терминал в браузере Yandex Cloud, но он **НЕ подключен к вашей VM**. Это отдельная среда.

### ✅ Правильный способ подключения

#### Вариант 1: Веб-консоль (рекомендуется)

1. На странице VM найдите кнопку **"Подключиться к ВМ по SSH"**
2. Или нажмите **"Подключиться"** → **"SSH"**
3. Вставьте приватный SSH ключ (который я дал ранее)
4. Логин: `ubuntu`
5. Нажмите "Подключиться"

#### Вариант 2: Через SSH клиент (с вашего компьютера)

```bash
ssh -i ~/.ssh/yandex-cloud ubuntu@51.250.105.190
```

## 🔧 Что нужно сделать после подключения

### 1. Проверить security groups

Порты должны быть открыты:
- **22** (SSH) - обычно открыт по умолчанию
- **4000** (API) - нужно открыть
- **8081** (Frontend) - нужно открыть (опционально)

### 2. Выполнить настройку

После подключения выполните:

```bash
cd ~/event_app_new/server
sudo systemctl start postgresql
sudo systemctl enable postgresql
npx prisma db push --accept-data-loss
pm2 delete event-app 2>/dev/null || true
pm2 start dist/main.js --name event-app
pm2 save
pm2 status
curl http://localhost:4000/health
```

## 📝 Резюме

**Ваша VM настроена правильно!** 

Нужно только:
1. Подключиться через веб-консоль (кнопка "Подключиться к ВМ по SSH")
2. Выполнить команды для запуска приложения
3. Проверить, что порты открыты в security groups

## ⚠️ Важно

- **НЕ используйте Cloud Shell** - это не подключение к вашей VM
- Используйте **"Подключиться к ВМ по SSH"** или веб-консоль
- После подключения вы окажетесь в терминале на вашей VM

