# 🔓 Открытие порта 22 для SSH

## ⚠️ Проблема

SSH подключение не работает из-за закрытого порта 22 в Security Groups.

## ✅ Решение: Открыть порт 22 через веб-консоль

### Шаги:

1. **Откройте [Yandex Cloud Console](https://console.cloud.yandex.ru)**

2. **Перейдите в VPC → Security Groups**

3. **Найдите группу безопасности**: `default-sg-enphv7p9hmf19tufcjd8`

4. **Нажмите на группу безопасности**

5. **Нажмите "Добавить правило"**

6. **Заполните форму:**
   - **Направление**: Входящий трафик (Ingress)
   - **Протокол**: TCP
   - **Порты**: 22
   - **Источник**: CIDR
   - **CIDR блоки**: `0.0.0.0/0` (или ваш IP для безопасности)
   - **Описание**: SSH access

7. **Нажмите "Сохранить"**

8. **Также добавьте правила для:**
   - **Порт 4000** (TCP) - для API
   - **Порт 8081** (TCP) - для Frontend (опционально)

## 🔄 После открытия портов

Подождите 1-2 минуты и попробуйте подключиться снова:

```bash
ssh -i ~/.ssh/yandex-cloud ubuntu@51.250.105.190
```

## 📋 Альтернатива: Использовать веб-консоль

Если не хотите открывать порты, используйте веб-консоль Yandex Cloud:

1. На странице VM нажмите **"Подключиться к ВМ по SSH"**
2. Вставьте приватный SSH ключ
3. Логин: `ubuntu`
4. Нажмите "Подключиться"

Веб-консоль работает без открытия портов!

## ✅ Проверка после открытия портов

После открытия портов проверьте:

```bash
# Проверка подключения
ssh -i ~/.ssh/yandex-cloud ubuntu@51.250.105.190

# Если подключились, выполните:
cd ~/event_app_new/server
sudo systemctl start postgresql
npx prisma db push --accept-data-loss
pm2 start dist/main.js --name event-app
pm2 save
pm2 status
```

