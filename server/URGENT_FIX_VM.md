# 🚨 СРОЧНОЕ РЕШЕНИЕ: Создание новой VM

## ✅ Что сделано

1. ✅ Удалена проблемная VM `event-app-backend`
2. ✅ Создана новая VM `event-app-backend-v2` с правильными настройками:
   - SSH ключ в метаданных
   - User-data с автоматическим запуском SSH
   - Правильная Security Group
   - Автоматическое открытие портов через ufw

## 🔍 Проверка новой VM

Выполните:

```bash
# Проверка статуса
yc compute instance get event-app-backend-v2 --format json | jq -r '.status, .network_interfaces[0].primary_v4_address.one_to_one_nat.address'

# Попытка SSH
NEW_IP=$(yc compute instance get event-app-backend-v2 --format json | jq -r '.network_interfaces[0].primary_v4_address.one_to_one_nat.address')
ssh -i ~/.ssh/yandex-cloud ubuntu@$NEW_IP
```

## 📋 Если SSH все еще не работает

### Вариант 1: Через веб-консоль (РЕКОМЕНДУЕТСЯ)

1. Откройте [Yandex Cloud Console](https://console.cloud.yandex.ru)
2. Compute Cloud → Виртуальные машины
3. Найдите `event-app-backend-v2`
4. Нажмите "Подключиться" → "SSH"
5. Используйте приватный ключ `~/.ssh/yandex-cloud`

### Вариант 2: Серийная консоль

1. В веб-консоли откройте VM
2. Вкладка "Серийная консоль"
3. Войдите как `ubuntu` (без пароля или попробуйте стандартные)

### Вариант 3: Проверка Security Groups

Убедитесь, что в Security Group `enpirb0nghvabvnv1asd` есть правила:
- Порт 22 (SSH) - TCP - 0.0.0.0/0
- Порт 4000 (API) - TCP - 0.0.0.0/0  
- Порт 8081 (Frontend) - TCP - 0.0.0.0/0

## 🎯 После успешного подключения

Выполните команды из `FINAL_SOLUTION.md` для настройки приложения.

