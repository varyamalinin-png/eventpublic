# 🎉 УСПЕХ! Приложение полностью настроено и работает!

## ✅ Что сделано

1. ✅ Создана новая VM `event-app-backend-v2` на Yandex Cloud
2. ✅ SSH подключение работает
3. ✅ Установлены все зависимости (Node.js 20, PostgreSQL, Redis, PM2)
4. ✅ Настроена база данных PostgreSQL
5. ✅ Клонирован репозиторий
6. ✅ Создан .env файл с правильными настройками
7. ✅ Установлены npm пакеты
8. ✅ Применены миграции Prisma
9. ✅ Приложение собрано
10. ✅ Приложение запущено через PM2
11. ✅ Приложение отвечает на запросы

## 🌐 Адреса

- **IP адрес VM**: `89.169.173.152`
- **API Base URL**: http://89.169.173.152:4000
- **API Endpoints**: http://89.169.173.152:4000/api/*

## 📱 Настройка мобильного приложения

Используйте в мобильном приложении:
- **Base URL**: `http://89.169.173.152:4000`

## ⚠️ Важно: Yandex Cloud Email API

Для отправки писем нужно добавить в `.env` на VM:

```bash
YANDEX_IAM_TOKEN=<ваш_iam_токен>
```

Получить токен можно через скрипт:
```bash
ssh -i ~/.ssh/yandex-cloud ubuntu@89.169.173.152
cd ~/event_app_new/server
node scripts/get-yandex-iam-token.js
```

Затем обновите .env и перезапустите:
```bash
pm2 restart event-app
```

## ⚠️ Важно: Yandex Object Storage

Для загрузки файлов нужно настроить Yandex Object Storage:

1. Создайте бакет в Yandex Object Storage
2. Получите Access Key и Secret Key
3. Добавьте в `.env` на VM:
   ```bash
   STORAGE_ACCESS_KEY=<ваш_access_key>
   STORAGE_SECRET_KEY=<ваш_secret_key>
   STORAGE_PUBLIC_BASE_URL=https://storage.yandexcloud.net/<bucket_name>
   ```
4. Перезапустите приложение:
   ```bash
   pm2 restart event-app
   ```

## 🔧 Полезные команды

### Подключение к VM:
```bash
ssh -i ~/.ssh/yandex-cloud ubuntu@89.169.173.152
```

### Проверка статуса приложения:
```bash
pm2 status
pm2 logs event-app
```

### Перезапуск приложения:
```bash
pm2 restart event-app
```

### Просмотр логов:
```bash
pm2 logs event-app --lines 50
```

### Проверка базы данных:
```bash
sudo -u postgres psql event_app
```

### Проверка Redis:
```bash
redis-cli ping
```

## 🎯 Статус

- ✅ VM создана и работает
- ✅ SSH подключение работает
- ✅ Приложение запущено и отвечает
- ⚠️ Email API не настроен (нужен YANDEX_IAM_TOKEN)
- ⚠️ Object Storage не настроен (нужны ключи доступа)

## 📋 Следующие шаги

1. ✅ Приложение работает - можно тестировать API
2. ⚠️ Настройте Yandex Cloud Email API для отправки писем
3. ⚠️ Настройте Yandex Object Storage для загрузки файлов
4. ⚠️ Настройте домен (опционально)
5. ⚠️ Настройте SSL сертификаты (опционально)

## 🆘 Если что-то не работает

1. **Проверьте статус PM2:**
   ```bash
   ssh -i ~/.ssh/yandex-cloud ubuntu@89.169.173.152 "pm2 status"
   ```

2. **Проверьте логи:**
   ```bash
   ssh -i ~/.ssh/yandex-cloud ubuntu@89.169.173.152 "pm2 logs event-app --lines 100"
   ```

3. **Проверьте порты:**
   ```bash
   ssh -i ~/.ssh/yandex-cloud ubuntu@89.169.173.152 "sudo netstat -tlnp | grep -E '4000|8081'"
   ```

4. **Проверьте Security Groups:**
   - Убедитесь, что порты 4000 и 8081 открыты в Security Group `enpirb0nghvabvnv1asd`

## ✅ Готово!

Ваше приложение теперь работает на Yandex Cloud и доступно из любой точки мира!

**IP адрес**: `89.169.173.152:4000`

