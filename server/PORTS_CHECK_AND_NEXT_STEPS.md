# 🔍 Проверка портов и следующие шаги

## ⚠️ Текущая ситуация

SSH подключение пока не работает. Это может быть из-за:
1. Правила Security Groups еще применяются (нужно подождать 2-5 минут)
2. Правила добавлены неправильно
3. Нужно перезапустить VM для применения правил

## ✅ Что проверить

### 1. Проверьте правила в Security Groups

1. Откройте [Yandex Cloud Console](https://console.cloud.yandex.ru)
2. VPC → Security Groups → `default-sg-enphv7p9hmf19tufcjd8`
3. Убедитесь, что в списке правил есть:
   - ✅ Порт 22 (TCP) - Ingress - 0.0.0.0/0
   - ✅ Порт 4000 (TCP) - Ingress - 0.0.0.0/0
   - ✅ Порт 8081 (TCP) - Ingress - 0.0.0.0/0

### 2. Подождите 2-5 минут

Правила Security Groups применяются не мгновенно. Подождите несколько минут.

### 3. Попробуйте перезапустить VM

Иногда нужно перезапустить VM для применения правил:

1. В консоли Yandex Cloud найдите вашу VM
2. Нажмите "Остановить" (Stop)
3. Подождите, пока VM остановится
4. Нажмите "Запустить" (Start)
5. Подождите 1-2 минуты
6. Попробуйте подключиться снова

## 🔌 Альтернатива: Используйте веб-консоль

Пока SSH не работает, используйте веб-консоль для настройки:

1. На странице VM нажмите **"Подключиться к ВМ по SSH"**
2. Вставьте приватный SSH ключ
3. Логин: `ubuntu`
4. Нажмите "Подключиться"

## 📋 Команды для выполнения в веб-консоли

После подключения через веб-консоль выполните:

```bash
# 1. Перейти в директорию проекта
cd ~/event_app_new/server

# 2. Запустить PostgreSQL
sudo systemctl start postgresql
sudo systemctl enable postgresql

# 3. Применить миграции
npx prisma db push --accept-data-loss

# 4. Запустить приложение
pm2 delete event-app 2>/dev/null || true
pm2 start dist/main.js --name event-app
pm2 save

# 5. Проверить статус
pm2 status

# 6. Проверить API локально
curl http://localhost:4000/health

# 7. Настроить автозапуск (скопируйте команду, которую выдаст PM2)
pm2 startup systemd -u ubuntu --hp /home/ubuntu
```

## ✅ После выполнения команд

Приложение будет доступно по адресу:
- **API**: http://51.250.105.190:4000
- **Health**: http://51.250.105.190:4000/health

## 🔄 Проверка SSH позже

Через 5-10 минут попробуйте подключиться через SSH снова:

```bash
ssh -i ~/.ssh/yandex-cloud ubuntu@51.250.105.190
```

Если подключится - отлично! Если нет - используйте веб-консоль.

## 📝 Резюме

1. ✅ Порты открыты в Security Groups
2. ⏳ Подождите 2-5 минут для применения правил
3. 🔄 Попробуйте перезапустить VM (опционально)
4. 🌐 Используйте веб-консоль для настройки приложения
5. ✅ После запуска приложение будет доступно на http://51.250.105.190:4000

