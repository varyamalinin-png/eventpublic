# 🔒 Настройка Security Group для новой VM

## ✅ Решение

Создана новая Security Group `event-app-sg` с правильными правилами и привязана к VM.

## 📋 Что сделано

1. ✅ Создана новая Security Group: `event-app-sg`
2. ✅ Добавлены правила для портов 22, 4000, 8081
3. ✅ Security Group привязана к VM
4. ✅ VM перезапущена

## 🔧 Если нужно добавить правила вручную

Если правила не добавились автоматически, добавьте их через веб-консоль:

1. Откройте [Yandex Cloud Console](https://console.cloud.yandex.ru)
2. VPC → Security Groups → `event-app-sg`
3. Добавьте правила:
   - **Порт 22** (TCP) - Ingress - 0.0.0.0/0 - SSH
   - **Порт 4000** (TCP) - Ingress - 0.0.0.0/0 - API
   - **Порт 8081** (TCP) - Ingress - 0.0.0.0/0 - Frontend

## 🔗 После настройки

Подождите 2-3 минуты и подключитесь:

```bash
ssh -i ~/.ssh/yandex-cloud ubuntu@84.201.179.62
```

## 📝 Команды для запуска приложения

После подключения:

```bash
cd ~/event_app_new/server
pm2 start dist/src/main.js --name event-app
pm2 save
pm2 status
curl http://localhost:4000/health
```

## ✅ Готово!

После настройки Security Group и запуска приложения, API будет доступен по адресу:
- **API**: http://84.201.179.62:4000
- **Health**: http://84.201.179.62:4000/health

