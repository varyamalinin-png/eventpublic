# ✅ Домен iventapp.ru настроен с SSL!

## ✅ Что сделано

1. ✅ DNS записи настроены и работают
2. ✅ Порт 80 и 443 открыты в Security Groups
3. ✅ nginx установлен и настроен
4. ✅ SSL сертификат получен от Let's Encrypt
5. ✅ nginx настроен для автоматического редиректа HTTP → HTTPS
6. ✅ Конфигурация приложения обновлена для использования домена
7. ✅ Приложение перезапущено

## 🌐 Адреса

- **Основной домен**: https://iventapp.ru
- **С www**: https://www.iventapp.ru
- **API**: https://iventapp.ru/api/*

## 📱 Обновление мобильного приложения

Теперь нужно обновить мобильное приложение:

1. **Откройте `client/app.json`**
2. **Измените `apiUrl`:**
   ```json
   "apiUrl": "https://iventapp.ru"
   ```
3. **Пересоберите приложение в Xcode:**
   ```bash
   cd client/ios
   open eventappnew.xcworkspace
   # Product → Clean Build Folder (Shift + Cmd + K)
   # Product → Build (Cmd + B)
   # Product → Run (Cmd + R)
   ```

## ✅ Проверка

Проверьте работу:

1. **HTTPS работает:**
   ```bash
   curl -I https://iventapp.ru
   ```

2. **API отвечает:**
   ```bash
   curl https://iventapp.ru/api/auth/check-email-status
   ```

3. **В браузере:**
   Откройте https://iventapp.ru - должен быть валидный SSL сертификат без предупреждений

## 🔄 Автоматическое обновление SSL

Certbot автоматически настроил обновление SSL сертификата. Проверить можно:

```bash
ssh -i ~/.ssh/yandex-cloud ubuntu@89.169.173.152
sudo certbot renew --dry-run
```

## 🛡️ Безопасность

Рекомендуется:

1. **Закрыть порт 4000** в Security Groups (оставить только 80 и 443)
2. **Настроить firewall на VM:**
   ```bash
   ssh -i ~/.ssh/yandex-cloud ubuntu@89.169.173.152
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   sudo ufw deny 4000/tcp
   sudo ufw enable
   ```

## 📋 Текущая конфигурация

- **Домен**: iventapp.ru
- **SSL**: Let's Encrypt (автоматическое обновление)
- **Nginx**: Reverse proxy на порт 4000
- **Приложение**: PM2, порт 4000
- **API URL**: https://iventapp.ru

## ✅ Готово!

Ваше приложение теперь доступно по безопасному HTTPS адресу https://iventapp.ru!

