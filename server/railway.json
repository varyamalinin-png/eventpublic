{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run prisma:generate && npm run build"
  },
  "deploy": {
    "startCommand": "npx prisma migrate resolve --applied 20250119180000_add_coordinates_to_events 2>/dev/null || true; npx prisma migrate resolve --applied 20250119190000_add_recurring_events_and_tags 2>/dev/null || true; npm run prisma:deploy || true; npm run start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
