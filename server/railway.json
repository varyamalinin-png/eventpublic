{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run prisma:generate && npm run build && ls -la dist/ || echo 'Build failed'"
  },
  "deploy": {
    "startCommand": "npx prisma migrate resolve --rolled-back 20250120130900_add_original_media_url 2>/dev/null || true; npx prisma migrate deploy || npx prisma db push || true; ls -la dist/ && node dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
