# Инструкция по тестированию отправки email

## ✅ Настройки добавлены

SMTP настройки уже добавлены в `server/.env`:
- **SMTP_HOST**: smtp.gmail.com
- **SMTP_PORT**: 587
- **SMTP_USER**: varya.malinin@gmail.com
- **EMAIL_VERIFICATION_REDIRECT_URL**: http://localhost:8081/(auth)/verify-email

## 🔄 Шаги для тестирования

### 1. Перезапустите сервер

Если сервер уже запущен, перезапустите его, чтобы загрузить новые настройки:

```bash
cd server
# Остановите текущий процесс (Ctrl+C) и запустите снова:
npm run start:dev
```

В логах должно появиться:
```
[MailerService] SMTP email service enabled (smtp.gmail.com:587)
```

### 2. Варианты тестирования

#### Вариант A: Тестирование через регистрацию

1. Откройте приложение (Expo) на телефоне/эмуляторе
2. Перейдите на экран регистрации
3. Зарегистрируйте нового пользователя с email `varya.malinin@gmail.com` или другим email
4. После регистрации должно появиться сообщение о необходимости подтвердить email
5. Проверьте почту `varya.malinin@gmail.com` - должно прийти письмо с подтверждением
6. Откройте письмо и перейдите по ссылке или скопируйте токен
7. Вставьте токен в приложении на экране подтверждения email

#### Вариант B: Тестирование через API (curl/Postman)

**Регистрация нового пользователя:**
```bash
curl -X POST http://localhost:4000/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "username": "testuser",
    "password": "testpassword123",
    "name": "Test User"
  }'
```

Должно вернуть:
```json
{
  "user": { ... },
  "requiresEmailVerification": true,
  "message": "Registration successful. Check your inbox to verify your email address."
}
```

**Проверьте почту `varya.malinin@gmail.com` - должно прийти письмо!**

**Повторная отправка письма подтверждения:**
```bash
curl -X POST http://localhost:4000/auth/resend-verification \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com"
  }'
```

#### Вариант C: Тестирование через приложение

1. Запустите Expo приложение:
   ```bash
   cd client
   npm start
   ```

2. Откройте приложение на телефоне/эмуляторе

3. Перейдите на экран регистрации: `/(auth)/login` → вкладка "Регистрация"

4. Заполните форму:
   - Email: `test@example.com` (или любой другой email)
   - Username: `testuser`
   - Пароль: `testpassword123`
   - Имя: `Test User`

5. Нажмите "Создать аккаунт"

6. После регистрации должно произойти:
   - Автоматический переход на экран подтверждения email
   - Письмо должно быть отправлено на указанный email

7. Проверьте почту `varya.malinin@gmail.com`:
   - Должно прийти письмо с темой "Подтвердите ваш e-mail"
   - В письме будет ссылка для подтверждения
   - Или токен для ввода вручную

8. Подтвердите email:
   - Перейдите по ссылке из письма (откроется в браузере)
   - Или скопируйте токен из письма и вставьте его в приложении

## 🔍 Проверка работы

### Проверьте логи сервера

После запуска сервера в логах должно быть:
```
[MailerService] SMTP email service enabled (smtp.gmail.com:587)
```

### Проверьте отправку письма

После регистрации в логах должно быть (без ошибок):
- Успешная отправка письма
- Или ошибка отправки (если что-то не так)

### Проверьте почту

1. Откройте почту `varya.malinin@gmail.com`
2. Проверьте папку "Входящие"
3. Если письма нет - проверьте папку "Спам"
4. В письме должны быть:
   - Ссылка для подтверждения
   - Токен для ввода вручную

## 🐛 Устранение проблем

### Проблема: "SMTP email service enabled" не появляется в логах

**Решение:**
- Проверьте, что переменные окружения правильно загружены
- Перезапустите сервер
- Проверьте файл `.env` - все переменные должны быть без пробелов

### Проблема: "Authentication failed" в логах

**Решение:**
- Проверьте, что пароль приложения правильный (без пробелов: `axrtupifmfogpjdr`)
- Убедитесь, что двухфакторная аутентификация включена в Google Account
- Проверьте, что пароль приложения создан для "Почты"

### Проблема: Письма не приходят

**Решение:**
1. Проверьте папку "Спам" в Gmail
2. Проверьте логи сервера на наличие ошибок отправки
3. Убедитесь, что email адрес получателя правильный
4. Попробуйте отправить письмо самому себе через `/auth/resend-verification`

### Проблема: "Connection timeout"

**Решение:**
- Проверьте, что порт 587 не заблокирован файрволом
- Попробуйте использовать порт 465 с `SMTP_SECURE=true`

## 📝 Пример тестового запроса

```bash
# Регистрация пользователя
curl -X POST http://localhost:4000/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "testuser@example.com",
    "username": "testuser123",
    "password": "SecurePassword123!",
    "name": "Test User",
    "accountType": "personal"
  }'

# Повторная отправка письма подтверждения
curl -X POST http://localhost:4000/auth/resend-verification \
  -H "Content-Type: application/json" \
  -d '{
    "email": "testuser@example.com"
  }'
```

## ✅ Что должно работать

- ✅ Регистрация нового пользователя → письмо отправляется
- ✅ Повторная отправка письма подтверждения → работает
- ✅ Сброс пароля → письмо отправляется
- ✅ Подтверждение email по токену → работает

Удачи в тестировании! 🚀

