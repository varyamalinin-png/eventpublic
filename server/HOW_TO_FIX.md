# 🔧 КАК ИСПРАВИТЬ ПРОБЛЕМУ ПРЯМО СЕЙЧАС

## ❌ Проблема

В логах видно, что на сервере запущена **СТАРАЯ версия** с Mailgun/SMTP:
```
[MailerService] 🔍 Mailgun check: enabled=true
[MailerService] ✅ Using Mailgun API
[MailerService] [SMTP] Using SMTP
[MailerService] ❌ Yandex Cloud error: fetch failed
```

## ✅ Что уже исправлено в коде

1. ✅ Удален весь код Mailgun/SMTP
2. ✅ Заменен `fetch` на `https` модуль
3. ✅ Обновлена команда сборки для очистки старых файлов

## 🚀 ЧТО НУЖНО СДЕЛАТЬ В RAILWAY

### Шаг 1: Очистить кеш и пересобрать

В Railway Dashboard:

1. Зайдите в ваш проект
2. **Settings** → **Build**
3. Нажмите **"Clear Build Cache"** (если есть кнопка)
4. ИЛИ нажмите **"Redeploy"** (Переразвернуть)

### Шаг 2: Или использовать принудительный пересборку

**Вариант A:** Через Git commit
```bash
git commit --allow-empty -m "Force rebuild - remove Mailgun/SMTP"
git push
```

**Вариант B:** Через Railway Dashboard
- **Deployments** → выберите последний deployment → **"Redeploy"**

### Шаг 3: Проверить переменные окружения

В Railway → **Variables** убедитесь что есть:
- `YANDEX_IAM_TOKEN` (обновить если истек - действует 12 часов!)
- `YANDEX_CLOUD_FROM_EMAIL` = `noreply@iventapp.ru`
- `YANDEX_CLOUD_API_ENDPOINT` = `https://mail-api.cloud.yandex.net` (опционально)

### Шаг 4: После деплоя проверить логи

**Должно быть:**
```
✅ Yandex Cloud Email API enabled (from: noreply@iventapp.ru)
[MailerService] ✅ Using Yandex Cloud Email API to send email to ...
```

**НЕ должно быть:**
- ❌ "Mailgun check"
- ❌ "Using Mailgun API"  
- ❌ "Using SMTP"
- ❌ "fetch failed"

## 📝 Обновленная команда сборки

Я обновил `railway.toml` и `nixpacks.toml` - теперь команда сборки автоматически очищает старую версию:
```
rm -rf dist node_modules/.cache && npm install --legacy-peer-deps && npm run prisma:generate && npm run build
```

## ⚠️ ВАЖНО

1. **IAM токен истекает через 12 часов!** Если письма не приходят, обновите:
   ```bash
   cd server && node scripts/get-yandex-iam-token.js
   ```
   Затем установите новый токен в Railway как `YANDEX_IAM_TOKEN`

2. После установки переменных **перезапустите сервис**

3. Если все еще видны Mailgun/SMTP в логах - значит старая версия не обновилась!

## 🧪 Тестирование

После деплоя:
1. Попробуйте зарегистрироваться
2. Проверьте логи - только Yandex Cloud
3. Проверьте почту - письмо должно прийти

---

**Полная инструкция:** см. `RAILWAY_DEPLOY_INSTRUCTIONS.md`

