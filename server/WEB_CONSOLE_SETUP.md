# 🌐 Завершение настройки через веб-консоль

## ⚠️ Важно

SSH подключение временно недоступно. Используйте веб-консоль Yandex Cloud для завершения настройки.

## 📋 Шаги

### 1. Откройте веб-консоль

1. Перейдите в [Yandex Cloud Console](https://console.cloud.yandex.ru)
2. Compute Cloud → Виртуальные машины
3. Найдите `event-app-backend`
4. Нажмите "Подключиться" → "SSH"
5. Используйте приватный ключ из `~/.ssh/yandex-cloud` (на вашем компьютере)

### 2. Выполните скрипт настройки

В веб-консоли выполните:

```bash
cd ~/event_app_new/server
cat > complete-setup.sh << 'EOF'
#!/bin/bash
set -e

echo "🚀 Завершение настройки приложения"

# Запуск PostgreSQL
sudo systemctl start postgresql
sudo systemctl enable postgresql
sleep 2

# Применение миграций
cd ~/event_app_new/server
npx prisma db push --accept-data-loss

# Запуск приложения
pm2 delete event-app 2>/dev/null || true
pm2 start dist/main.js --name event-app
pm2 save

# Ожидание запуска
sleep 5

# Проверка
pm2 status
curl -s http://localhost:4000/health || echo "API еще не отвечает"

echo "✅ Готово! API: http://51.250.105.190:4000"
EOF

chmod +x complete-setup.sh
./complete-setup.sh
```

### 3. Или выполните команды вручную

```bash
# 1. Запустить PostgreSQL
sudo systemctl start postgresql
sudo systemctl enable postgresql

# 2. Применить миграции
cd ~/event_app_new/server
npx prisma db push --accept-data-loss

# 3. Запустить приложение
pm2 delete event-app 2>/dev/null || true
pm2 start dist/main.js --name event-app
pm2 save

# 4. Настроить автозапуск
pm2 startup systemd -u ubuntu --hp /home/ubuntu
# Скопируйте и выполните команду, которую выдаст PM2

# 5. Проверить статус
pm2 status
pm2 logs event-app

# 6. Проверить API
curl http://localhost:4000/health
```

### 4. Проверка работы

После выполнения скрипта:

1. **Проверьте статус PM2:**
   ```bash
   pm2 status
   ```

2. **Проверьте логи:**
   ```bash
   pm2 logs event-app
   ```

3. **Проверьте API локально:**
   ```bash
   curl http://localhost:4000/health
   ```

4. **Проверьте API извне:**
   Откройте в браузере: http://51.250.105.190:4000/health

## 📱 Настройка мобильного приложения

После успешного запуска API:

1. В мобильном приложении укажите backend URL:
   ```
   http://51.250.105.190:4000
   ```

2. Убедитесь, что CORS настроен (в `.env` должно быть `CORS_ORIGIN=*`)

## 🔧 Полезные команды

```bash
# Перезапустить приложение
pm2 restart event-app

# Остановить приложение
pm2 stop event-app

# Посмотреть логи
pm2 logs event-app

# Проверить базу данных
sudo -u postgres psql -d event_app -c "\dt"
```

## ⚠️ Если что-то пошло не так

1. **Проверьте логи:**
   ```bash
   pm2 logs event-app --lines 50
   ```

2. **Проверьте .env файл:**
   ```bash
   cd ~/event_app_new/server
   cat .env
   ```

3. **Проверьте PostgreSQL:**
   ```bash
   sudo systemctl status postgresql
   ```

4. **Проверьте Redis:**
   ```bash
   redis-cli ping
   ```

## ✅ После успешной настройки

Приложение будет доступно по адресу:
- **API**: http://51.250.105.190:4000
- **Health**: http://51.250.105.190:4000/health

Используйте этот адрес в мобильном приложении!

