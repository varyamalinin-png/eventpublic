# 🚀 Инструкция по деплою на Railway

## Проблема

На сервере запущена **старая версия** с Mailgun/SMTP. Нужно задеплоить **новую версию** с только Yandex Cloud Email API.

## Шаги для деплоя новой версии

### 1. Очистить кеш сборки в Railway

1. Зайдите в **Railway Dashboard**
2. Выберите ваш проект
3. Перейдите в **Settings** → **Build**
4. Нажмите **"Clear Build Cache"** (Очистить кеш сборки)
5. ИЛИ используйте кнопку **"Redeploy"** (Переразвернуть)

### 2. Принудительная пересборка

В Railway Dashboard:
- Перейдите в **Deployments**
- Найдите последний deployment
- Нажмите **"Redeploy"**
- ИЛИ в Settings → Build → **"Trigger New Build"**

### 3. Проверить переменные окружения

В Railway Dashboard → **Variables** убедитесь что установлены:

```
YANDEX_IAM_TOKEN=<ваш_iam_токен>
YANDEX_CLOUD_FROM_EMAIL=noreply@iventapp.ru
YANDEX_CLOUD_API_ENDPOINT=https://mail-api.cloud.yandex.net
```

**ВАЖНО:** IAM токен действителен **12 часов**! Если истек:
```bash
cd server && node scripts/get-yandex-iam-token.js
```

### 4. После деплоя проверить логи

В логах должны быть:
```
✅ Yandex Cloud Email API enabled (from: noreply@iventapp.ru)
[MailerService] ✅ Using Yandex Cloud Email API to send email to ...
```

**НЕ должно быть:**
- ❌ "Mailgun check"
- ❌ "Using Mailgun API"
- ❌ "Using SMTP"
- ❌ "fetch failed"

### 5. Тестирование

1. Попробуйте зарегистрироваться с новым email
2. Проверьте логи - должны быть только сообщения о Yandex Cloud
3. Проверьте почту - письмо должно прийти

## Если проблема осталась

### Вариант 1: Ручная очистка через Railway CLI

```bash
# Установите Railway CLI
npm i -g @railway/cli

# Войдите
railway login

# Выберите проект
railway link

# Очистите кеш и пересоберите
railway up --detach
```

### Вариант 2: Принудительная пересборка через git

```bash
# Сделайте коммит (даже пустой)
git commit --allow-empty -m "Force rebuild - remove Mailgun/SMTP"
git push
```

Railway автоматически запустит новую сборку.

### Вариант 3: Использовать Metal Build Environment

В Railway Dashboard:
- Settings → Build → **Builder**
- Попробуйте переключиться на **Metal Build Environment (Beta)**
- Это может помочь избежать проблем с кешем

## Проверка что новая версия задеплоена

После деплоя проверьте:
1. Логи содержат только Yandex Cloud (нет Mailgun/SMTP)
2. Регистрация работает и письма отправляются
3. Нет ошибок "fetch failed" (используется https модуль)

## Важно

- **IAM токен истекает через 12 часов** - обновляйте его регулярно
- После установки переменных **перезапустите сервис**
- Если видны упоминания Mailgun/SMTP - значит старая версия все еще запущена!

