# 🔍 Где найти кнопку подключения в веб-консоли

## 📋 Где искать кнопку подключения

В веб-консоли Yandex Cloud кнопка подключения может находиться в разных местах:

### 1. На странице VM (вкладка "Обзор")

- В правом верхнем углу может быть кнопка **"Подключиться"** или **"Connect"**
- Или в разделе **"Доступ"** / **"Access"**

### 2. В меню действий (три точки или меню)

- Нажмите на **три точки** (⋮) или **меню** рядом с названием VM
- Там может быть пункт **"Подключиться"** / **"Connect"**

### 3. В разделе "Доступ" / "Access"

- Откройте вкладку **"Доступ"** или **"Access"** на странице VM
- Там может быть кнопка подключения

### 4. Через серийную консоль

- Найдите раздел **"Серийная консоль"** / **"Serial Console"**
- Или **"Последовательный порт"** / **"Serial Port"**

## 🔄 Альтернатива: Подождать и использовать SSH

Правила Security Groups могут применяться до 10-15 минут.

**Подождите еще 5-10 минут** и попробуйте подключиться через SSH:

```bash
ssh -i ~/.ssh/yandex-cloud ubuntu@51.250.105.190
```

## 📸 Что проверить в веб-консоли

1. **Верхняя панель** на странице VM - может быть кнопка "Подключиться"
2. **Правая панель** - может быть блок с кнопкой подключения
3. **Вкладки** - проверьте все вкладки на странице VM
4. **Меню действий** - три точки или меню рядом с VM

## ⚡ Быстрое решение: Используйте SSH через терминал

Если кнопки нет, просто подождите еще 5-10 минут и используйте SSH напрямую:

```bash
ssh -i ~/.ssh/yandex-cloud ubuntu@51.250.105.190
```

Порты открыты, правила применяются, нужно просто подождать.

## 📝 После подключения

Как только подключитесь, выполните:

```bash
cd ~/event_app_new/server
sudo systemctl start postgresql
sudo systemctl enable postgresql
npx prisma db push --accept-data-loss
pm2 delete event-app 2>/dev/null || true
pm2 start dist/main.js --name event-app
pm2 save
pm2 status
curl http://localhost:4000/health
```

## ✅ Рекомендация

**Просто подождите 10-15 минут** после открытия портов и попробуйте SSH снова. Правила Security Groups применяются не мгновенно.

