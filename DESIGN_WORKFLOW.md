# 🎨 Workflow работы с дизайном через Figma

## Вариант 1: Использование Design Tokens (Рекомендуется)

### Шаг 1: Импорт в Figma
1. Установите плагин **"Figma Tokens"** или **"Design Tokens"** в Figma
2. Откройте файл `client/constants/designTokens.json`
3. В Figma: Plugins → Figma Tokens → Import → выберите `designTokens.json`
4. Токены появятся в Figma как переменные

### Шаг 2: Редактирование в Figma
- Измените цвета, шрифты, отступы в Figma
- Все изменения сохраняются в токенах

### Шаг 3: Экспорт обратно
1. В Figma: Plugins → Figma Tokens → Export
2. Сохраните как `designTokens.json`
3. Отправьте мне файл или закоммитьте в git
4. Я применю изменения в код автоматически

---

## Вариант 2: Прямое редактирование (Проще)

### Просто редактируйте файл `client/constants/designTokens.ts`

Например, измените:
```typescript
colors: {
  primary: '#8B5CF6',  // ← измените на нужный цвет
  // ...
}
```

После изменений скажите мне "примени дизайн-токены" и я обновлю все компоненты.

---

## Вариант 3: HTML/CSS → Figma (Для визуального редактирования)

### Если хотите видеть компоненты в Figma:

1. **Плагин "HTML to Figma"**:
   - Установите плагин в Figma
   - Я могу сгенерировать HTML версию компонента
   - Импортируйте в Figma
   - Редактируйте
   - Экспортируйте стили обратно

2. **Плагин "React to Figma"**:
   - Аналогично, но для React компонентов

---

## 📋 Текущие токены в проекте

Все токены находятся в:
- `client/constants/designTokens.ts` - TypeScript версия (для кода)
- `client/constants/designTokens.json` - JSON версия (для Figma)

### Что можно редактировать:
- ✅ Все цвета (primary, background, text, accent, etc.)
- ✅ Размеры шрифтов (fontSize)
- ✅ Веса шрифтов (fontWeight)
- ✅ Отступы (spacing)
- ✅ Радиусы скругления (borderRadius)
- ✅ Тени (shadows)

---

## 🚀 Быстрый старт

1. Откройте `client/constants/designTokens.ts`
2. Измените нужные значения (цвета, размеры, отступы)
3. Скажите мне: "примени дизайн-токены"
4. Я обновлю все компоненты автоматически

Или используйте Figma Tokens плагин для визуального редактирования!

