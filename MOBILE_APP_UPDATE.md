# 📱 Обновление мобильного приложения после смены сервера

## ✅ Что сделано

1. ✅ Обновлен API URL в `app.json` с Railway на Yandex Cloud
2. ✅ Добавлен IP адрес Yandex Cloud в настройки iOS для разрешения HTTP подключений

## 🔄 Изменения

### Старый URL (Railway):
- `https://eventpublic-production.up.railway.app`

### Новый URL (Yandex Cloud):
- `http://89.169.173.152:4000`

## 📋 Что нужно сделать

### 1. Пересобрать приложение в Xcode

**ВАЖНО:** После изменения `app.json` нужно пересобрать приложение, чтобы изменения вступили в силу.

#### Шаги:

1. **Откройте проект в Xcode:**
   ```bash
   cd client/ios
   open eventappnew.xcworkspace
   ```
   Или откройте `client/ios/eventappnew.xcworkspace` через Finder

2. **Очистите сборку (Clean Build):**
   - В Xcode: `Product` → `Clean Build Folder` (или `Shift + Cmd + K`)

3. **Пересоберите приложение:**
   - В Xcode: `Product` → `Build` (или `Cmd + B`)
   - Затем: `Product` → `Run` (или `Cmd + R`) для установки на устройство

4. **Или через Expo CLI:**
   ```bash
   cd client
   npx expo prebuild --clean
   npx expo run:ios
   ```

### 2. Проверка подключения

После установки приложения на телефон:

1. Откройте приложение
2. Попробуйте зарегистрироваться или войти
3. Проверьте, что запросы идут на новый сервер

### 3. Проверка логов

В консоли Xcode или в логах приложения должно быть:
```
[API] Using API URL: http://89.169.173.152:4000
```

## ⚠️ Важно

1. **HTTP вместо HTTPS:** Новый сервер использует HTTP (не HTTPS), поэтому добавлен IP в `NSExceptionDomains` для iOS
2. **IP адрес может измениться:** Если IP адрес VM изменится, нужно будет обновить `app.json` и пересобрать приложение
3. **Для продакшена:** Рекомендуется настроить домен и SSL сертификат для использования HTTPS

## 🔧 Альтернатива: Использование переменной окружения

Если хотите менять URL без пересборки, можно использовать `.env` файл:

1. Создайте `client/.env`:
   ```
   EXPO_PUBLIC_API_URL=http://89.169.173.152:4000
   ```

2. Приложение автоматически подхватит эту переменную (но все равно нужно пересобрать для Release сборки)

## ✅ Готово!

После пересборки приложение будет подключаться к новому серверу на Yandex Cloud.

**Новый API URL**: `http://89.169.173.152:4000`

