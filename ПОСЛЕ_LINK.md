# ✅ После выбора проекта `joyful-miracle`

## 📋 Что нужно сделать дальше:

### Шаг 1: Выберите `joyful-miracle` в терминале

Используйте стрелки ↑↓ чтобы выбрать `joyful-miracle`, затем нажмите Enter.

---

### Шаг 2: Проверка связи

После выбора выполните:

```bash
npx -y @railway/cli status
```

Должно показать:
```
Project: joyful-miracle
Environment: production
Service: ...
```

---

### Шаг 3: Установка переменных

После успешной связи запустите:

```bash
cd /Users/varya.malinina.2003mail.ru/event_app_new
./railway-auto-setup.sh
```

Это автоматически установит все необходимые переменные окружения.

---

### Шаг 4: Проверка в веб-интерфейсе

1. Откройте https://railway.app
2. Откройте проект `joyful-miracle`
3. Убедитесь, что есть:
   - ✅ PostgreSQL сервис
   - ✅ Redis сервис
   - ❓ Backend сервис (если нет - создайте через "GitHub Repo")

4. Если создаете backend сервис:
   - "+ New" → "GitHub Repo"
   - Выберите ваш репозиторий
   - Settings → Root Directory = `server`
   - Variables должны автоматически иметь `DATABASE_URL` и `REDIS_URL`

---

### Шаг 5: Деплой

```bash
git add .
git commit -m "Railway setup"
git push
```

Railway автоматически задеплоит!

---

**Выберите `joyful-miracle` и продолжите!** 🚀

