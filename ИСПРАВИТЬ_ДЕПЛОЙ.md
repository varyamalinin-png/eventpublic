# 🔧 Как исправить деплой на Railway

## ❌ Проблема:

Backend сервис не задеплоился, хотя PostgreSQL и Redis работают.

---

## ✅ Решение через веб-интерфейс:

### Шаг 1: Создайте сервис для backend

1. Откройте https://railway.app
2. Откройте ваш проект
3. Нажмите **"+ New"**
4. Выберите **"GitHub Repo"** (НЕ "Database")
5. Найдите `varyamalinin-png/eventpublic`
6. Нажмите на него

### Шаг 2: Настройте Root Directory

**ВАЖНО!** Без этого ничего не заработает:

1. Откройте созданный сервис (backend)
2. Перейдите в **"Settings"**
3. Найдите **"Root Directory"** или **"Working Directory"**
4. Введите: **`server`**
5. Нажмите **"Save"** или **"Update"**

### Шаг 3: Проверьте переменные

1. В настройках сервиса → **"Variables"**
2. Railway должен автоматически добавить:
   - `DATABASE_URL` (из PostgreSQL)
   - `REDIS_URL` (из Redis)
3. Если их нет - добавьте вручную, скопировав из PostgreSQL/Redis

### Шаг 4: Добавьте остальные переменные

Если еще не добавлены:

```bash
NODE_ENV=production
PORT=4000
CORS_ORIGIN=*

JWT_ACCESS_SECRET=EIGUZBTMbqW2OD2my1Gk9qUdVs3XFo5MgI1YY1aXYTE=
JWT_REFRESH_SECRET=oBo5isGfN6UoUEG+cXl1GJDHBpU6RuGoOvyiAWhX2E8=
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

APP_BACKEND_BASE_URL=https://eventpublic-production.up.railway.app
```

### Шаг 5: Дождитесь деплоя

Railway автоматически начнет сборку после настройки.

---

## 📊 После деплоя проверьте:

1. **Вкладка "Deployments"** - должен быть статус "Success"
2. **Логи** - ищите `Backend listening on port 4000`
3. **PostgreSQL** - должны появиться таблицы (после миграций)
4. **URL** - откройте `https://eventpublic-production.up.railway.app/health`

---

## ⚠️ Частые ошибки:

### Ошибка: "Root Directory not found"
- Убедитесь, что Root Directory = `server` (не `server/` с слешем)

### Ошибка: "Cannot find package.json"
- Root Directory должен быть `server` (где лежит package.json)

### Ошибка: "DATABASE_URL not found"
- Добавьте вручную, скопировав из PostgreSQL → Variables

### Деплой падает
- Проверьте логи в Railway → Deployments → View Logs

---

**Самое важное: Root Directory = `server`!** ✅

