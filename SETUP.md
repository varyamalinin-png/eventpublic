# 🚀 Настройка Railway с нуля - Пошаговая инструкция

## ✅ Текущее состояние:

- ✅ Railway проект: `joyful-miracle`
- ✅ Сервис: `eventpublic`
- ✅ Переменные окружения: все установлены
- ✅ GitHub репозиторий: `varyamalinin-png/eventpublic`
- ✅ railway.json: настроен
- ⚠️ Нужно проверить: подключение GitHub к сервису

---

## 📋 ШАГ 1: Проверка подключения GitHub

### В Railway веб-интерфейсе:

1. Откройте https://railway.app
2. Проект: `joyful-miracle`
3. Сервис: `eventpublic`
4. Перейдите в **Settings** → **Source** (или **Deploy**)

### Проверьте:

- [ ] Есть ли поле "GitHub Repo" или "Connected Repo"?
- [ ] Подключен ли репозиторий `varyamalinin-png/eventpublic`?
- [ ] Установлен ли **Root Directory** = `server`?
- [ ] Установлен ли **Branch** = `main`?
- [ ] Включен ли **Auto Deploy**?

---

## 📋 ШАГ 2: Если GitHub НЕ подключен

### Вариант A: Подключить существующий репозиторий

1. В Railway → `eventpublic` → **Settings** → **Source**
2. Нажмите **"Connect Repo"** или **"Connect GitHub"**
3. Выберите репозиторий: `varyamalinin-png/eventpublic`
4. Установите:
   - **Root Directory:** `server`
   - **Branch:** `main`
5. Нажмите **"Deploy"** или **"Save"**

### Вариант B: Создать новый репозиторий

Если текущий репозиторий не работает:

1. Создайте новый репозиторий на GitHub:
   - Название: `event-app-backend` (или любое другое)
   - Private или Public
2. Загрузите код:

```bash
cd /Users/varya.malinina.2003mail.ru/event_app_new
git remote remove origin
git remote add origin https://github.com/varyamalinin-png/НОВОЕ_НАЗВАНИЕ.git
git push -u origin main
```

3. В Railway подключите новый репозиторий

---

## 📋 ШАГ 3: Проверка переменных окружения

В Railway → `eventpublic` → **Variables** проверьте наличие:

- ✅ `DATABASE_URL` (должен быть автоматически из PostgreSQL)
- ✅ `REDIS_URL` (должен быть автоматически из Redis)
- ✅ `PORT=4000`
- ✅ `NODE_ENV=production`
- ✅ `CORS_ORIGIN=*`
- ✅ `JWT_ACCESS_SECRET`
- ✅ `JWT_REFRESH_SECRET`
- ✅ `JWT_ACCESS_TTL=15m`
- ✅ `JWT_REFRESH_TTL=7d`
- ✅ `APP_BACKEND_BASE_URL=https://eventpublic-production.up.railway.app`

**Если чего-то нет - добавьте вручную.**

---

## 📋 ШАГ 4: Триггер деплоя

### После подключения GitHub:

Railway должен автоматически начать деплой. Если нет:

1. В Railway → `eventpublic` → **Settings** → **Deploy**
2. Нажмите **"Redeploy"** или **"Deploy"**

Или через git:

```bash
cd /Users/varya.malinina.2003mail.ru/event_app_new
git commit --allow-empty -m "Trigger deploy"
git push
```

---

## 📋 ШАГ 5: Проверка деплоя

1. В Railway → `eventpublic` → **Deployments**
2. Должен быть новый деплой со статусом:
   - **Building** (сборка)
   - **Deploying** (деплой)
   - **Success** ✅ (готово)

3. Откройте логи: **Deployments** → выберите деплой → **View Logs**

Ищите:
- ✅ `npm install` - установка зависимостей
- ✅ `npm run prisma:generate` - генерация Prisma
- ✅ `npm run build` - сборка
- ✅ `npm run prisma:deploy` - миграции
- ✅ `Backend listening on port 4000` - сервер запустился

---

## 📋 ШАГ 6: Проверка работоспособности

После успешного деплоя:

1. Откройте в браузере:
   ```
   https://eventpublic-production.up.railway.app/health
   ```

2. Должен вернуться:
   ```json
   {"status":"ok"}
   ```

3. Если ошибка - проверьте логи в Railway

---

## 🔍 Возможные проблемы:

### Проблема: "No deployments found"
**Решение:** GitHub репозиторий не подключен - выполните ШАГ 2

### Проблема: "Cannot find package.json"
**Решение:** Root Directory должен быть `server`, а не корень проекта

### Проблема: "Build failed"
**Решение:** Проверьте логи - возможно ошибка в коде или зависимостях

### Проблема: "Database connection failed"
**Решение:** Проверьте `DATABASE_URL` в Variables

---

**Начните с ШАГ 1 - проверьте подключение GitHub!** 🚀

