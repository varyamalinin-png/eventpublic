# 🚀 Настройка удаленного сервера (бесплатно)

## Вариант 1: Railway.app (рекомендуется)

### Шаг 1: Регистрация

1. Откройте https://railway.app
2. Нажмите "Start a New Project"
3. Войдите через GitHub (бесплатно)

### Шаг 2: Создание проекта

1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите ваш репозиторий (или создайте новый)
4. Выберите папку `server` как корневую

### Шаг 3: Настройка базы данных

1. В проекте нажмите "+ New"
2. Выберите "Database" → "PostgreSQL"
3. Railway автоматически создаст базу данных
4. Скопируйте `DATABASE_URL` из переменных окружения

### Шаг 4: Настройка Redis

1. В проекте нажмите "+ New"
2. Выберите "Database" → "Redis"
3. Скопируйте `REDIS_URL` из переменных окружения

### Шаг 5: Настройка переменных окружения

В настройках проекта → "Variables" добавьте:

```bash
NODE_ENV=production
PORT=4000
CORS_ORIGIN=*

DATABASE_URL=<из шага 3>
REDIS_URL=<из шага 4>

JWT_ACCESS_SECRET=<сгенерируйте случайную строку минимум 16 символов>
JWT_REFRESH_SECRET=<сгенерируйте другую случайную строку минимум 16 символов>
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

APP_BACKEND_BASE_URL=<URL вашего Railway проекта>
APP_FRONTEND_BASE_URL=https://your-app-domain.com

# Email (опционально, можно оставить пустым)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_SECURE=false

# Storage - используем AWS S3 (бесплатный tier) или оставляем пустым
STORAGE_DRIVER=s3
STORAGE_BUCKET=event-app-media
STORAGE_REGION=us-east-1
STORAGE_ACCESS_KEY=<AWS Access Key>
STORAGE_SECRET_KEY=<AWS Secret Key>
STORAGE_PUBLIC_BASE_URL=https://<bucket>.s3.amazonaws.com
```

### Шаг 6: Настройка сборки

В настройках проекта → "Settings" → "Build Command":
```bash
npm install && npm run prisma:generate && npm run build
```

"Start Command":
```bash
npm run prisma:deploy && npm run start:prod
```

### Шаг 7: Получение URL

1. Railway автоматически создаст публичный URL
2. Скопируйте URL (например: `https://your-project.railway.app`)
3. Обновите `APP_BACKEND_BASE_URL` в переменных окружения

---

## Вариант 2: Render.com (альтернатива)

### Шаг 1: Регистрация

1. Откройте https://render.com
2. Войдите через GitHub

### Шаг 2: Создание Web Service

1. Нажмите "New" → "Web Service"
2. Подключите ваш GitHub репозиторий
3. Настройки:
   - **Root Directory:** `server`
   - **Build Command:** `npm install && npm run prisma:generate && npm run build`
   - **Start Command:** `npm run prisma:deploy && npm run start:prod`
   - **Environment:** `Node`

### Шаг 3: База данных

1. "New" → "PostgreSQL"
2. Скопируйте `DATABASE_URL`

### Шаг 4: Redis

1. "New" → "Redis"
2. Скопируйте `REDIS_URL`

### Шаг 5: Переменные окружения

Добавьте те же переменные, что и для Railway

---

## Вариант 3: Fly.io (для продвинутых)

### Шаг 1: Установка CLI

```bash
curl -L https://fly.io/install.sh | sh
```

### Шаг 2: Регистрация

```bash
fly auth signup
```

### Шаг 3: Создание приложения

```bash
cd server
fly launch
```

### Шаг 4: Настройка базы данных

```bash
fly postgres create
fly redis create
```

### Шаг 5: Переменные окружения

```bash
fly secrets set DATABASE_URL=<url>
fly secrets set REDIS_URL=<url>
# ... остальные переменные
```

---

## 📝 После настройки

### Обновите приложение:

1. Откройте `client/app.json`
2. Обновите `extra.apiUrl` на URL вашего удаленного сервера:
   ```json
   "extra": {
     "apiUrl": "https://your-project.railway.app"
   }
   ```

3. Добавьте домен в `NSExceptionDomains`:
   ```json
   "your-project.railway.app": {
     "NSExceptionAllowsInsecureHTTPLoads": false,
     "NSIncludesSubdomains": true
   }
   ```

4. Пересоберите приложение в Xcode

---

## 💰 Стоимость

- **Railway:** $5/месяц бесплатно (потом платно)
- **Render:** Бесплатно (с ограничениями)
- **Fly.io:** Бесплатно (с ограничениями)

---

## ⚠️ Важно

- После настройки сервер будет работать 24/7
- Не нужно держать MacBook включенным
- URL будет постоянным (не меняется при перезапуске)

---

**Рекомендую Railway.app - самый простой вариант!** 🚀

