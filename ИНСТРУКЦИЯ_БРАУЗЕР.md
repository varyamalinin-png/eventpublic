# 🔐 Авторизация в Railway через `--browserless`

## Что такое `--browserless`?

Это режим авторизации **БЕЗ автоматического открытия браузера**.

Вместо автоматического открытия:
1. CLI покажет вам **ссылку** и **токен**
2. Вы откроете ссылку в браузере **вручную**
3. Вставите токен на странице
4. Готово!

---

## 📋 Инструкция:

### Выполните команду:

```bash
cd /Users/varya.malinina.2003mail.ru/event_app_new/server
npx -y @railway/cli login --browserless
```

### Что вы увидите в терминале:

```
Login to your Railway account

Visit this URL to authenticate:
https://railway.app/cli/login?token=ABC123XYZ...

Enter the token displayed above:
```

### Что делать:

1. **Скопируйте ссылку** (URL) из терминала
   - Она будет типа: `https://railway.app/cli/login?token=ABC123XYZ...`

2. **Откройте эту ссылку в браузере**

3. **Скопируйте токен** из терминала
   - Это будет строка после `token=` в URL

4. **Вставьте токен** на странице Railway в браузере

5. **Нажмите "Authorize"** или "Войти"

6. **Вернитесь в терминал** — авторизация должна завершиться автоматически

---

## ✅ После авторизации:

Проверьте:

```bash
npx -y @railway/cli whoami
```

Должно показать ваше имя/email.

Затем выполните:

```bash
cd /Users/varya.malinina.2003mail.ru/event_app_new
./railway-auto-setup.sh
```

---

**Просто выполните команду и следуйте инструкциям на экране!** 🚀

