# 🚀 Выполните эти команды в терминале:

## Шаг 1: Авторизация (один раз)

```bash
cd /Users/varya.malinina.2003mail.ru/event_app_new/server
npx -y @railway/cli login
```

Откроется браузер - авторизуйтесь через GitHub.

---

## Шаг 2: Инициализация проекта (один раз)

```bash
cd /Users/varya.malinina.2003mail.ru/event_app_new/server
npx -y @railway/cli init
```

Выберите:
- **"Link to existing project"** → выберите ваш проект

---

## Шаг 3: Автоматическая настройка переменных

```bash
cd /Users/varya.malinina.2003mail.ru/event_app_new
./railway-auto-setup.sh
```

Этот скрипт автоматически:
- ✅ Проверит авторизацию
- ✅ Установит все переменные окружения
- ✅ Покажет статус

---

## Шаг 4: Проверка в веб-интерфейсе

1. Откройте https://railway.app
2. Откройте ваш проект
3. Откройте backend сервис (или создайте новый через "GitHub Repo")
4. В **Settings** → **Source** → **Root Directory** установите: `server`
5. В **Variables** проверьте:
   - `DATABASE_URL` (должен быть автоматически из PostgreSQL)
   - `REDIS_URL` (должен быть автоматически из Redis)
   - Остальные переменные

---

## Шаг 5: Деплой

```bash
cd /Users/varya.malinina.2003mail.ru/event_app_new
git add .
git commit -m "Railway setup"
git push
```

Railway автоматически задеплоит после push!

---

## ✅ Проверка:

```bash
# Статус
cd server && npx -y @railway/cli status

# Логи
cd server && npx -y @railway/cli logs

# Переменные
cd server && npx -y @railway/cli variables
```

---

**Выполните команды выше по порядку!** 🚀

